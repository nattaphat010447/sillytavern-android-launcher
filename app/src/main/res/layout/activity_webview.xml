<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.standroid.launcher.ui.TopEdgeSwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <WebView
            android:id="@+id/webView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </com.standroid.launcher.ui.TopEdgeSwipeRefreshLayout>

    <!-- Loading overlay — dark purple gradient with spinner + node log -->
    <LinearLayout
        android:id="@+id/loadingOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_gradient_main"
        android:orientation="vertical">

        <!-- Top section: spinner + title + status (takes ~40% of height) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="2"
            android:gravity="center"
            android:orientation="vertical">

            <!-- Spinner -->
            <ProgressBar
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:indeterminateTint="@color/purple_500" />

            <!-- App name above status -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="STANDROID"
                android:textColor="@color/text_primary"
                android:textSize="18sp"
                android:textStyle="bold"
                android:letterSpacing="0.12" />

            <!-- Status text -->
            <TextView
                android:id="@+id/tvWaitStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:paddingStart="20dp"
                android:paddingEnd="20dp"
                android:text="Waiting for SillyTavern…"
                android:textColor="@color/text_muted"
                android:textSize="13sp"
                android:gravity="center"
                android:lineSpacingMultiplier="1.4" />

        </LinearLayout>

        <!-- Node log card (takes ~60% of height) -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardNodeLog"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="3"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            app:cardBackgroundColor="@color/bg_surface"
            app:cardCornerRadius="16dp"
            app:cardElevation="4dp"
            app:strokeColor="@color/bg_card_stroke"
            app:strokeWidth="1dp">

            <ScrollView
                android:id="@+id/scrollNodeLog"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:fillViewport="true"
                android:background="@color/bg_elevated">

                <TextView
                    android:id="@+id/tvNodeLog"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:fontFamily="monospace"
                    android:padding="10dp"
                    android:textColor="@color/text_secondary"
                    android:textSize="10sp"
                    android:lineSpacingMultiplier="1.3" />

            </ScrollView>

        </com.google.android.material.card.MaterialCardView>

    </LinearLayout>

</FrameLayout>
